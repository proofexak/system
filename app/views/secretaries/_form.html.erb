<%= form_for @secretary do |f| %>
<div class="panel-body">
  <div class="form-group">
    <%= f.label :telephone_number %><br>
    <%= f.text_field :telephone_number %>
  </div>
  <div class="actions">
    <%= f.submit @secretary.persisted? ? "Update" : "Finish", class: "btn btn-primary" %>
  </div>
</div>

<% if @secretary.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@secretary.errors.count, "error") %> prohibited this secretary from being saved:</h2>

      <ul>
      <% @secretary.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<% end %>
